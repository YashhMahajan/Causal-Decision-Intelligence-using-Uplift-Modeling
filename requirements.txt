# Pinned deps for the causal_prep preprocessing pipeline (mirrors pyproject.toml).
# Python 3.11.  Install with either:
#   pip install -e .          (preferred — makes `python -m causal_prep.run` work)
#   pip install -r requirements.txt
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
scipy==1.17.1
pyarrow==25.0.1
openpyxl==3.1.5
matplotlib==3.11.1
joblib==1.6.0
